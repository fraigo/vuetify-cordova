import menuItems from './menuItems'
import menuButtons from './menuButtons'

var menuConfig = {
  items: menuItems,
  buttons: menuButtons,
  miniVariant: false,
  toolbar: true,
  darkMode: false,
  opened: false
}

export default menuConfig
